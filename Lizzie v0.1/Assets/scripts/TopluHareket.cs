using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TopluHareket : MonoBehaviour
{
    public Transform connectedItem;
   

    
   /* void Update()
    {
        transform.position = new Vector3(
            Mathf.Lerp(transform.position.x,connectedItem.position.x,Time.deltaTime*20),
            0,
            connectedItem.position.z + 1

            ); ;
    }
   */
}
